name: Multi Jobs
on: workflow_dispatch
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Running tests..."

  deploy:
    runs-on: ubuntu-latest
    needs: test          # ← only runs if "test" passes
    steps:
      - run: echo "Deploying..."
