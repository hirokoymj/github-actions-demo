## Practice Test - 31-40

- [Practice Test - 31-40](#practice-test---31-40)
  - [Q31: What jobs.job_id.if conditional will make sure that job production-deploy is triggered only on my-org/my-repo repository? (Select two.)](#q31-what-jobsjob_idif-conditional-will-make-sure-that-job-production-deploy-is-triggered-only-on-my-orgmy-repo-repository-select-two)
  - [Q32: What GitHub-hosted runner types are available to use? (Select three.)](#q32-what-github-hosted-runner-types-are-available-to-use-select-three)
  - [Q33: Is this statement true? Not all steps run actions, but all actions run as a step](#q33-is-this-statement-true-not-all-steps-run-actions-but-all-actions-run-as-a-step)
  - [Q34: For any action published in GitHub Marketplace, you can often use it in multiple versions, which approach is the most stable and secure?](#q34-for-any-action-published-in-github-marketplace-you-can-often-use-it-in-multiple-versions-which-approach-is-the-most-stable-and-secure)
  - [Q35: To prevent a job from failure when one of the steps fails you can include:](#q35-to-prevent-a-job-from-failure-when-one-of-the-steps-fails-you-can-include)
  - [Q36: You defined a matrix job example_matrix. How can limit the matrix to run a maximum of 2 jobs at a time?](#q36-you-defined-a-matrix-job-example_matrix-how-can-limit-the-matrix-to-run-a-maximum-of-2-jobs-at-a-time)
  - [Q37: Which of these is a proper way of setting an output parameter PET with a value of DOG in a step.](#q37-which-of-these-is-a-proper-way-of-setting-an-output-parameter-pet-with-a-value-of-dog-in-a-step)
  - [Q38: Which of these is a way of using action_state in step_two?](#q38-which-of-these-is-a-way-of-using-action_state-in-step_two)
  - [Q39: Is this statement true? Workflows can be reused, but a reusable workflow cannot call another reusable workflow.](#q39-is-this-statement-true-workflows-can-be-reused-but-a-reusable-workflow-cannot-call-another-reusable-workflow)
  - [Q40: In the following example, workflow A passes all of its secrets to workflow B, by using the inherit keyword. Then workflow B calls workflow C. Which statement regarding secrets is true for that example?](#q40-in-the-following-example-workflow-a-passes-all-of-its-secrets-to-workflow-b-by-using-the-inherit-keyword-then-workflow-b-calls-workflow-c-which-statement-regarding-secrets-is-true-for-that-example)
  - [Summary](#summary)

### Q31: What jobs.job_id.if conditional will make sure that job production-deploy is triggered only on my-org/my-repo repository? (Select two.)

```
  jobs:
    production-deploy:
      if: <CONDITION>
      runs-on: ubuntu-latest
      steps:
        ...
```

- `if: ${{ github.repository == 'my-org/my-repo' }}`
- `if: ${{ github.organization == 'my-org' && github.repository == 'my-repo' }}`
- `if: github.repository == 'my-org/my-repo'`
- `if: ${{ github.org == 'my-org' && github.repository == 'my-repo' }}`

💡 https://docs.github.com/en/actions/learn-github-actions/contexts#github-context

✅ Correct Answers (two):

```
- `if: ${{ github.repository == 'my-org/my-repo' }}` ✅
- `if: ${{ github.organization == 'my-org' && github.repository == 'my-repo' }}` ❌
- `if: github.repository == 'my-org/my-repo'` ✅
- `if: ${{ github.org == 'my-org' && github.repository == 'my-repo' }}` ❌
```

- These two are actually doing the exact same thing — GitHub Actions allows you to write if conditions with or without the `${{ }}` expression syntax:
- ❌ github.organization does not exist — it's a made-up context variable
- ❌ github.org does not exist either — also made-up

---

### Q32: What GitHub-hosted runner types are available to use? (Select three.)

- Android
- macOS
- Windows
- Ubuntu Linux

💡 https://docs.github.com/en/actions/using-workflows/workflow-syntax-for-github-actions#choosing-github-hosted-runners

✅ Correct Answers:

```
- Android ❌
- macOS ✅
- Windows ✅
- Ubuntu Linux ✅
```

```yaml
jobs:
  build:
    runs-on: ubuntu-latest # or windows-latest or macos-latest
```

- Linux (Ubuntu) + Windows + macOS = GitHub-hosted runners
- ❌ Android — GitHub does not offer a hosted Android runner. If you need to build/test Android apps, you run it on Ubuntu and install the Android SDK yourself, or use a third-party service like Firebase Test Lab.

---

### Q33: Is this statement true? Not all steps run actions, but all actions run as a step

- True
- False

💡 https://docs.github.com/en/actions/using-workflows/workflow-syntax-for-github-actions#jobsjob_idsteps

✅ Correct Answer:

```
- True✅
- False
```

```yaml
steps:
  # This step runs an ACTION
  - name: Checkout code
    uses: actions/checkout@v4

  # This step runs a SHELL COMMAND (not an action)
  - name: Print message
    run: echo "Hello World"
```

- "Not all steps run actions" ✅
  - A step can either run an **action** OR a **shell command** directly using `run`:
- "All actions run as a step" ✅

---

### Q34: For any action published in GitHub Marketplace, you can often use it in multiple versions, which approach is the most stable and secure?

- Reference a version tag
- Reference the main branch
- Reference the commit SHA

💡 https://docs.github.com/en/actions/using-workflows/workflow-syntax-for-github-actions#example-using-versioned-actions

✅ Correct Answer:

```
- Reference a version tag ❌
- Reference the main branch❌
- Reference the commit SHA ✅
```

✅ Reference the commit SHA

```yaml
# ✅ Most secure — pinned to exact commit, immutable
- uses: actions/checkout@a81bbbf8298c0fa03ea29cdc473d45769f953675

# ⚠️ Less secure — tag can be deleted and recreated
- uses: actions/checkout@v4

# ❌ Least secure — always changes
- uses: actions/checkout@main
```

---

### Q35: To prevent a job from failure when one of the steps fails you can include:

```yaml
#1- continue-on-error flag in the failing step
steps:
  - uses: my-org/failing-action@v1
    continue-on-error: true


#2- ignore-error flag in the failing step
steps:
  - uses: my-org/failing-action@v1
    ignore-error: true ❌ Does not exist in GitHub Actions — made-up keyword


#3- failure() conditional in the failing step
steps:
  - uses: my-org/failing-action@v1
    if: failure()


#4- always() conditional in the failing step
steps:
  - uses: my-org/failing-action@v1
    if: always()
```

💡 https://docs.github.com/en/actions/using-workflows/workflow-syntax-for-github-actions#jobsjob_idstepscontinue-on-error

✅ Correct Answer: continue-on-error: true

```yaml
#1- continue-on-error flag in the failing step
steps:
  - uses: my-org/failing-action@v1
    continue-on-error: true

#2 - ignore-error: true - ❌ Does not exist in GitHub Actions
#3 - if: failure() - ❌ This means "only run this step IF a previous step failed"
#4 - if: always() - ❌ This means "always run this step regardless"
```

---

### Q36: You defined a matrix job example_matrix. How can limit the matrix to run a maximum of 2 jobs at a time?

```yaml
jobs:
  example_matrix:
    strategy:
      matrix:
        version: [10, 12, 14]
        os: [ubuntu-latest, windows-latest]
```

- Set jobs.example_matrix.strategy.concurrency to 2
- Set jobs.example_matrix.strategy.max-parallel to 2

- 💡 https://docs.github.com/en/actions/using-workflows/workflow-syntax-for-github-actions#jobsjob_idstrategymax-parallel

✅ Correct Answer:

```
- Set jobs.example_matrix.strategy.concurrency to 2
- Set jobs.example_matrix.strategy.max-parallel to 2 ✅
```

```yaml
jobs:
  example_matrix:
    strategy:
      max-parallel: 2 ✅
      matrix:
        version: [10, 12, 14]
        os: [ubuntu-latest, windows-latest]
```

- This tells GitHub Actions to run at most 2 matrix jobs simultaneously, even if more runners are available.

---

### Q37: Which of these is a proper way of setting an output parameter PET with a value of DOG in a step.

- gh set-output "PET=DOG"
- echo "DOG=PET" >> "$GITHUB_OUTPUT"
- echo "PET=DOG" >> "$GITHUB_OUTPUT"
- gh set-output "DOG=PET"

💡 https://docs.github.com/en/actions/using-workflows/workflow-commands-for-github-actions#setting-an-output-parameter

✅ Correct Answer:

```
- gh set-output "PET=DOG"
- echo "DOG=PET" >> "$GITHUB_OUTPUT"
- echo "PET=DOG" >> "$GITHUB_OUTPUT" ✅
- gh set-output "DOG=PET"
```

- echo `"PET=DOG" >> "$GITHUB_OUTPUT"`

```yaml
steps:
  - name: Set output
    id: my-step
    run: echo "PET=DOG" >> "$GITHUB_OUTPUT"

  - name: Use output
    run: echo "The pet is ${{ steps.my-step.outputs.PET }}"
```

---

### Q38: Which of these is a way of using action_state in step_two?

```yaml
  steps:
    - name: Set the value
      id: step_one
      run: |
        echo "action_state=yellow" >> "$GITHUB_ENV"
    - name: Use the value
      id: step_two
      run: ?
```

- run: echo "$steps.step_one.outputs.action_state"
- run: echo "${{ action_state }}"
- run: echo "$action_state" ✅
- run: echo "${{ steps.step_one.outputs.action_state }}" ❌

💡 https://docs.github.com/en/actions/using-workflows/workflow-commands-for-github-actions#example-of-writing-an-environment-variable-to-github_env

✅ Correct Answer:

```
- run: echo "$steps.step_one.outputs.action_state"
- run: echo "${{ action_state }}"
- run: echo "$action_state" ✅
- run: echo "${{ steps.step_one.outputs.action_state }}" ❌
```

- `$GITHUB_ENV` vs `$GITHUB_OUTPUT` are different!

| Method         | How to set                            | How to read                       |
| -------------- | ------------------------------------- | --------------------------------- |
| $GITHUB_ENV    | echo "NAME=value" >> "$GITHUB_ENV"    | $NAME (plain env variable)        |
| $GITHUB_OUTPUT | echo "NAME=value" >> "$GITHUB_OUTPUT" | ${{ steps.step_id.outputs.NAME }} |

---

### Q39: Is this statement true? Workflows can be reused, but a reusable workflow cannot call another reusable workflow.

- False
- True

💡 https://docs.github.com/en/actions/using-workflows/reusing-workflows#nesting-reusable-workflows

✅ Correct Answer:

```
- False✅
- True
```

- Why False
  - GitHub Actions does support nesting reusable workflows — a reusable workflow can call another reusable workflow. This is called nesting.
- nesting is allowed, just limited to 9 levels deep.

---

### Q40: In the following example, workflow A passes all of its secrets to workflow B, by using the inherit keyword. Then workflow B calls workflow C. Which statement regarding secrets is true for that example?

```yaml
jobs:
  workflowA-calls-workflowB:
    uses: octo-org/example-repo/.github/workflows/B.yml@main
    secrets: inherit
```

```yaml
jobs:
  workflowB-calls-workflowC:
    uses: different-org/example-repo/.github/workflows/C.yml@main
```

- All secrets available to workflow A will be also available to workflow B, but not to workflow C ✅
- All secrets available to workflow A will be also available to workflow B and workflow C
- Only repository and environment secrets available to workflow A will be available to workflow B, but not to workflow C. Organization scoped secrets cannot be inherited
- All secrets from octo-org organization and octo-org/example-repo repository will be available to workflow B, but not to workflow C

💡 https://docs.github.com/en/actions/using-workflows/reusing-workflows#passing-secrets-to-nested-workflows

✅ Correct Answer:

```
- All secrets available to workflow A will be also available to workflow B, but not to workflow C ✅
- All secrets available to workflow A will be also available to workflow B and workflow C
- Only repository and environment secrets available to workflow A will be available to workflow B, but not to workflow C. Organization scoped secrets cannot be inherited
- All secrets from octo-org organization and octo-org/example-repo repository will be available to workflow B, but not to workflow C
```

```yaml
# Workflow A → Workflow B  (uses inherit ✅)
jobs:
  workflowA-calls-workflowB:
    uses: octo-org/example-repo/.github/workflows/B.yml@main
    secrets: inherit   # ← explicitly passes ALL secrets to B

# Workflow B → Workflow C  (NO secrets passed ❌)
jobs:
  workflowB-calls-workflowC:
    uses: different-org/example-repo/.github/workflows/C.yml@main
    # no secrets: inherit here!
```

- Workflow A → Workflow B (✅ inherit all) → Workflow C (❌ no inherit - get NO secrets)

### Summary

- "Not all steps run actions" ✅ - A step == action OR shell `run`:
- Step: action ==> steps.name(xxx).uses
- Step: shell ==> steps.name(xxx).run
- continue-on-error: true
- if: always() - always run this step regardless
