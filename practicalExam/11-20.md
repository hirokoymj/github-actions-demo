## Practice Test - 11-20

- [Practice Test - 11-20](#practice-test---11-20)
  - [Q11: In a workflow with multiple jobs the default behavior is:](#q11-in-a-workflow-with-multiple-jobs-the-default-behavior-is)
  - [Q12: If job B requires job A to be finished you have to:](#q12-if-job-b-requires-job-a-to-be-finished-you-have-to)
  - [Q13: In a workflow with multiple jobs, if job A fails then:](#q13-in-a-workflow-with-multiple-jobs-if-job-a-fails-then)
  - [Q14: This code will launch 6 different jobs in parallel using the matrix strategy. Can you use the matrix strategy to parallelize entire workflows?](#q14-this-code-will-launch-6-different-jobs-in-parallel-using-the-matrix-strategy-can-you-use-the-matrix-strategy-to-parallelize-entire-workflows)
  - [Q15: Which matrix job definition is syntactically correct?](#q15-which-matrix-job-definition-is-syntactically-correct)
  - [Q16: How do you access matrix variables in a matrix strategy job?](#q16-how-do-you-access-matrix-variables-in-a-matrix-strategy-job)
  - [Q17: When using the pull_request and pull_request_target events, how do you configure the workflow to run only when targeting the prod branch?](#q17-when-using-the-pull_request-and-pull_request_target-events-how-do-you-configure-the-workflow-to-run-only-when-targeting-the-prod-branch)
  - [Q18: This workflow will run on all pull requests where:](#q18-this-workflow-will-run-on-all-pull-requests-where)
  - [Q19: Fill in the blank: When using push event trigger filters you can use `<____>` patterns to target multiple branches](#q19-fill-in-the-blank-when-using-push-event-trigger-filters-you-can-use-____-patterns-to-target-multiple-branches)
  - [Q20: Which event allows you to manually trigger a workflow from the GitHub UI?](#q20-which-event-allows-you-to-manually-trigger-a-workflow-from-the-github-ui)
  - [Summary](#summary)

### Q11: In a workflow with multiple jobs the default behavior is:

- Only the first job runs, others require manual approval
- All jobs run in parallel
- Jobs run based on the order they are defined in the workflow file
- Jobs run in sequence

💡 https://docs.github.com/en/actions/using-workflows/about-workflows#creating-dependent-jobs

✅ Correct answer:

```
- Only the first job runs, others require manual approval
- All jobs run in parallel
- Jobs run based on the order they are defined in the workflow file
- Jobs run in sequence
👉 All jobs run in parallel
```

💡 Why this is correct
By default, jobs in a workflow run in parallel
This helps speed up execution

👉 Example:

```yaml
jobs:
  job1:
    runs-on: ubuntu-latest
  job2:
    runs-on: ubuntu-latest
```

➡️ job1 and job2 start at the same time

🔄 When do jobs run in sequence?

Only when you explicitly define dependencies using needs:

```yaml
jobs:
  job1:
    runs-on: ubuntu-latest

  job2:
    runs-on: ubuntu-latest
    needs: job1 # 👈 runs AFTER job1
```

---

### Q12: If job B requires job A to be finished you have to:

- use the requires keyword in job A to create this dependency
- use the needs keyword in job B to create this dependency
- use the needs keyword in job A to create this dependency
- use the requires keyword in job B to create this dependency

💡 https://docs.github.com/en/actions/using-workflows/about-workflows#creating-dependent-jobs

✅ Correct answer:

```
- use the requires keyword in job A to create this dependency
- use the needs keyword in job B to create this dependency ✅
- use the needs keyword in job A to create this dependency
- use the requires keyword in job B to create this dependency
```

💡 Why this is correct
The needs keyword defines job dependencies

🧩 Example

```yaml
jobs:
  jobA:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Job A"

  jobB:
    runs-on: ubuntu-latest
    needs: jobA # 👈 waits for jobA
    steps:
      - run: echo "Job B"
```

---

### Q13: In a workflow with multiple jobs, if job A fails then:

- the jobs that are dependent on job A are skipped
- the workflow immediately cancels all other jobs
- the jobs that are dependent on job A fail

💡 https://docs.github.com/en/actions/how-tos/write-workflows/choose-what-workflows-do/use-jobs#defining-prerequisite-jobs

✅ Correct answer:

```
- the jobs that are dependent on job A are skipped ✅
- the workflow immediately cancels all other jobs
- the jobs that are dependent on job A fail
```

🧩 Example

```yaml
jobs:
  jobA:
    runs-on: ubuntu-latest
    steps:
      - run: exit 1 # ❌ fails

  jobB:
    runs-on: ubuntu-latest
    needs: jobA # depends on jobA
    steps:
      - run: echo "Job B"
```

👉 Result:

- jobA → ❌ failed
- jobB → ⏭️ skipped

---

### Q14: This code will launch 6 different jobs in parallel using the matrix strategy. Can you use the matrix strategy to parallelize entire workflows?

```yaml
jobs:
  example_matrix:
    strategy:
      matrix:
        version: [10, 12, 14]
        os: [ubuntu-latest, windows-latest]
```

- Only if the workflows are in the same repository
- No
- Yes
- Only with self-hosted runners

💡 https://docs.github.com/en/actions/using-workflows/reusing-workflows#using-a-matrix-strategy-with-a-reusable-workflow

✅ Correct answer:

```
- Only if the workflows are in the same repository
- No
- Yes✅
- Only with self-hosted runners
```

- Yes is the correct answer.
  GitHub Actions supports using the matrix strategy with reusable workflows, meaning you can parallelize entire workflows — not just jobs. The link in the question even points to the official docs confirming this: you can call a reusable workflow from a matrix job, which effectively runs multiple instances of the entire reusable workflow in parallel.
- When a matrix strategy runs, GitHub Actions launches all the job combinations simultaneously (at the same time), rather than one after another.

```yaml
## Using a matrix strategy with a reusable workflow
jobs:
  ReusableMatrixJobForDeployment:
    strategy:
      matrix:
        target: [dev, stage, prod]
    uses: octocat/octo-repo/.github/workflows/deployment.yml@main
    with:
      target: ${{ matrix.target }}
```

---

### Q15: Which matrix job definition is syntactically correct?

```yaml
#1
jobs:
  matrix:
    version: [10, 12, 14]
    os: [ubuntu-latest, windows-latest]

#2
jobs:
  example_matrix:
    strategy:
      matrix:
        version: [10, 12, 14]
        os: [ubuntu-latest, windows-latest]

#3
jobs:
  example_matrix:
    matrix:
      version: [10, 12, 14]
      os: [ubuntu-latest, windows-latest]

#4
jobs:
  example_matrix:
    matrix:
      strategy:
        version: [10, 12, 14]
        os: [ubuntu-latest, windows-latest]
```

💡 https://docs.github.com/en/actions/using-jobs/using-a-matrix-for-your-jobs#using-a-matrix-strategy

✅ Correct answer:

```yaml
#1
jobs:
  matrix:
    version: [10, 12, 14]
    os: [ubuntu-latest, windows-latest]

#2 ✅
jobs:
  example_matrix:
    strategy:
      matrix:
        version: [10, 12, 14]
        os: [ubuntu-latest, windows-latest]
#3
#4
```

💡 Why this is correct

- In GitHub Actions, matrix must be defined under strategy
- `jobs → job_id → strategy → matrix`

---

### Q16: How do you access matrix variables in a matrix strategy job?

- Using the job context
- Using the matrix context
- Using the jobs context
- Using the vars context

💡 https://docs.github.com/en/actions/using-jobs/using-a-matrix-for-your-jobs#using-a-matrix-strategy

✅ Correct answer:

```
- Using the job context
- Using the matrix context ✅
- Using the jobs context
- Using the vars context
👉 Using the matrix context
```

💡 Why this is correct

Matrix variables are accessed via the matrix context
Syntax:

```yaml
${{ matrix.<variable_name> }}
```

```yaml
jobs:
  example_matrix:
    strategy:
      matrix:
        version: [10, 12]
        os: [ubuntu-latest]

    runs-on: ${{ matrix.os }}

    steps:
      - run: echo "Node version is ${{ matrix.version }}"

# 👉 This will output:
#- Node version is 10
#- Node version is 12
```

---

### Q17: When using the pull_request and pull_request_target events, how do you configure the workflow to run only when targeting the prod branch?

- Using glob patterns
- Using branches filter
- You create the workflow only on prod branch
- Using branch filter

💡 https://docs.github.com/en/actions/using-workflows/triggering-a-workflow#using-filters-to-target-specific-branches-for-pull-request-events

✅ Correct answer:

```
- Using glob patterns
- Using branches filter ✅
- You create the workflow only on prod branch
- Using branch filter
👉 Using branches filter
```

💡 Why this is correct

To run a workflow only when a PR targets a specific branch (e.g., prod), you use the branches filter:

```yaml
on:
  pull_request:
    branches:
      - prod
```

---

### Q18: This workflow will run on all pull requests where:

```yaml
on:
  pull_request:
    branches:
      - 'release/**'
      - '!release/**-alpha'
```

- the source branch name starts with release but does not end with -alpha
- the target branch name starts with release but does not end with -alpha
- the source branch name starts with release
- the target branch name starts with release

💡 https://docs.github.com/en/actions/using-workflows/triggering-a-workflow#example-including-and-excluding-branches

✅ Correct answer:

```
- the source branch name starts with release but does not end with -alpha
- the target branch name starts with release but does not end with -alpha ✅
- the source branch name starts with release
- the target branch name starts with release
```

💡 Why this is correct

For pull_request:

- The branches filter applies to the target (base) branch, not the source branch
- !!! If you want, I can show you a visual diagram (source vs target branch in PR)—this is a very common confusion 👍

---

### Q19: Fill in the blank: When using push event trigger filters you can use `<____>` patterns to target multiple branches

- scheme
- action
- glob
- regex

💡 https://docs.github.com/en/actions/using-workflows/triggering-a-workflow#using-filters-to-target-specific-branches-or-tags-for-push-events

✅ Correct answer:

```
- scheme
- action
- glob ✅
- regex
```

💡 Why this is correct

GitHub Actions uses **glob patterns** to match branches or tags in filters.

```yaml
on:
  push:
    branches:
      - 'feature/*'
      - 'release/**'
```

🔍 What is a glob pattern?

- `*` → matches any characters (except /)
- `**` → matches across directories (/)
- `!` → excludes patterns

---

### Q20: Which event allows you to manually trigger a workflow from the GitHub UI?

- workflow_run
- workflow_dispatch
- repository_dispatch
- manual_trigger

💡 https://docs.github.com/en/actions/using-workflows/manually-running-a-workflow

✅ Correct answer:

```
- workflow_run
- workflow_dispatch ✅
- repository_dispatch
- manual_trigger
```

### Summary

- matrix: GitHub Actions supports using the matrix strategy with reusable workflows, meaning you can parallelize entire workflows.
- `jobs → job_id → strategy → matrix`
