## Practice Test - 1-10

- [Practice Test - 1-10](#practice-test---1-10)
  - [Q1: Which statement is correct regarding passing permissions to reusable workflows?](#q1-which-statement-is-correct-regarding-passing-permissions-to-reusable-workflows)
  - [Q2: What are the different permission levels you can assign to GITHUB_TOKEN in the permissions block?](#q2-what-are-the-different-permission-levels-you-can-assign-to-github_token-in-the-permissions-block)
  - [Q3: You can use permissions to modify the GITHUB_TOKEN permissions on: (Select two.)](#q3-you-can-use-permissions-to-modify-the-github_token-permissions-on-select-two)
  - [Q4: Are GitHub Actions free for public repositories?](#q4-are-github-actions-free-for-public-repositories)
  - [Q5: Which of these is not a valid event that could trigger a workflow?](#q5-which-of-these-is-not-a-valid-event-that-could-trigger-a-workflow)
  - [Q6: Which is true about workflows? (Select three.)](#q6-which-is-true-about-workflows-select-three)
  - [Q7: Which components are required for a workflow? (Select two.)](#q7-which-components-are-required-for-a-workflow-select-two)
  - [Q8: Which event is triggered by a webhook action from outside of the repository?](#q8-which-event-is-triggered-by-a-webhook-action-from-outside-of-the-repository)
  - [Q9: Workflows are defined in which format](#q9-workflows-are-defined-in-which-format)
  - [Q10: Where should you store sensitive data such as passwords or certificates that will be used in workflows](#q10-where-should-you-store-sensitive-data-such-as-passwords-or-certificates-that-will-be-used-in-workflows)
  - [Summary](#summary)
    - [GITHUB_TOKEN Summary](#github_token-summary)

---

### Q1: Which statement is correct regarding passing permissions to reusable workflows?

- The GITHUB_TOKEN permissions passed from the caller workflow can be neither downgraded nor elevated by the called workflow.
- The GITHUB_TOKEN permissions passed from the caller workflow can be only elevated by the called workflow.
- The GITHUB_TOKEN permissions passed from the caller workflow can be both downgraded and elevated by the called workflow.
- The GITHUB_TOKEN permissions passed from the caller workflow can be only downgraded by the called workflow.

💡 https://docs.github.com/en/actions/using-workflows/reusing-workflows#access-and-permissions

✅ Correct answer:

```
- The GITHUB_TOKEN permissions passed from the caller workflow can be neither downgraded nor elevated by the called workflow.❌
- The GITHUB_TOKEN permissions passed from the caller workflow can be only elevated❌ by the called workflow.❌
- The GITHUB_TOKEN permissions passed from the caller workflow can be both downgraded and elevated❌ by the called workflow.❌
- The GITHUB_TOKEN permissions passed from the caller workflow can be only downgraded by the called workflow. ✅
```

```yaml
# .github/workflows/caller.yml
name: Caller Workflow

on: workflow_dispatch

permissions:
  contents: write # 👈 MAX permission allowed

jobs:
  call-reusable:
    uses: ./.github/workflows/reusable.yml
```

- **What is GITHUB_TOKEN?**
  GITHUB_TOKEN is an automatically generated secret that GitHub creates at the start of every workflow run. It's a short-lived token scoped to your repository that allows your workflow to authenticate with the GitHub API — things like creating releases, commenting on PRs, pushing commits, etc. You never have to create or store it manually; GitHub handles its lifecycle.
- never elevate them
- GITHUB_TOKEN permissions in reusable workflows only flow downward, never upward.
- GITHUB_TOKEN permissions: none, write, read
- GITHUB_TOKEN permissions: ✅WF level, ✅Job level, ❌ Step level

---

### Q2: What are the different permission levels you can assign to GITHUB_TOKEN in the permissions block?

- none, write, read
- read, write
- read, write, delete

✅ Correct answer: 👉 none, read, write

```
- none, write, read ✅
- read, write
- read, write, delete
```

💡 https://docs.github.com/en/actions/using-jobs/assigning-permissions-to-jobs

💡 **Why this is correct**

In GitHub Actions, each permission for GITHUB_TOKEN can be set to:

- read → read-only access
- write → read + write access
- none → completely disabled

```yaml
permissions:
  contents: read # Can read repository contents
  issues: write # Can create/update issues
  actions: none # Has no access to Actions API
```

---

### Q3: You can use permissions to modify the GITHUB_TOKEN permissions on: (Select two.)

- Step level
- Workflow level
- Job level

💡 https://docs.github.com/en/actions/writing-workflows/choosing-what-your-workflow-does/controlling-permissions-for-github_token

✅ Correct answers:

```
- Step level
- Workflow level✅
- Job level✅
```

```yaml
name: Permissions Demo

on:
  push:
    branches: [main]

# Workflow-level permissions — applies to ALL jobs by default
permissions:
  contents: read
  issues: write

jobs:
  job-one:
    runs-on: ubuntu-latest
    # No permissions block here, so it inherits the workflow-level permissions:
    # contents: read, issues: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

  job-two:
    runs-on: ubuntu-latest
    # Job-level permissions — completely OVERRIDES the workflow-level block
    # This job now only has pull-requests: write (loses contents and issues access)
    permissions:
      pull-requests: write
    steps:
      - name: Comment on PR
        run: echo "I can write to PRs, but not issues or contents"
```

---

### Q4: Are GitHub Actions free for public repositories?

- No, only self-hosted runners are free for public repositories
- No, all GitHub Actions usage is billed
- Yes, but only for the first 2,000 minutes per month
- Yes, when using standard GitHub-hosted runners

💡 https://docs.github.com/en/billing/concepts/product-billing/github-actions#how-use-of-github-actions-is-measured

✅ Correct answer: 👉 Yes, when using standard GitHub-hosted runners

```
- No, only self-hosted runners are free for public repositories
- No, all GitHub Actions usage is billed
- Yes, but only for the first 2,000 minutes per month
- Yes, when using standard GitHub-hosted runners ✅
```

**For public repositories:**

- GitHub Actions is completely free
- This includes standard GitHub-hosted runners (like ubuntu-latest)
- There is no minutes limit for public repos

---

### Q5: Which of these is not a valid event that could trigger a workflow?

- A branch is created
- Adding a label to a pull request
- Cloning the repository
- Committing a file to master branch

💡 https://docs.github.com/en/actions/using-workflows/events-that-trigger-workflows#about-events-that-trigger-workflows

✅ Correct answers (Select three):

```
- A branch is created✅
- Adding a label to a pull request✅
- Cloning the repository
- Committing a file to master branch✅
```

**💡 Why this is correct**

GitHub Actions workflows are triggered by GitHub events (things that happen on GitHub), such as:

- push → committing code
- pull_request → PR actions (like adding labels)
- create → creating a branch or tag
- Cloning a repository is a local action, not a GitHub event → So it cannot trigger a workflow

---

### Q6: Which is true about workflows? (Select three.)

- Workflows are written in any of .yaml, .json or .toml formats
- Workflow can run only one job at a time
- Workflows can run one or multiple jobs at a time
- Workflows can be triggered manually, by an event or run on a schedule
- Workflows can be shared in GitHub Marketplace
- Workflows have to be defined in the .github/workflows directory
- Workflows can only be run on a schedule

💡 https://docs.github.com/en/actions/using-workflows/about-workflows

✅ Correct answers (Select three):

```
- Workflows are written in any of .yaml, .json or .toml formats
- Workflow can run only one job at a time
- Workflows can run one or multiple jobs at a time ✅
- Workflows can be triggered manually, by an event or run on a schedule ✅
- Workflows can be shared in GitHub Marketplace
- Workflows have to be defined in the .github/workflows directory ✅
- Workflows can only be run on a schedule
```

- 👉 Workflows can run one or multiple jobs at a time
- 👉 Workflows can be triggered manually, by an event or run on a schedule
- 👉 Workflows have to be defined in the .github/workflows directory

🧠 Quick summary

- ✔ Multiple jobs
- ✔ Multiple triggers (event / manual / schedule)
- ✔ Must be in .github/workflows
- ❌ Not shared in Marketplace

---

### Q7: Which components are required for a workflow? (Select two.)

- Defined branches on which the workflow will run
- One or more events that will trigger the workflow
- One or more jobs
- Workflow name

💡 https://docs.github.com/en/actions/using-workflows/about-workflows#workflow-basics

✅ Correct answers (Select two):

```
- Defined branches on which the workflow will run
- One or more events that will trigger the workflow✅
- One or more jobs✅
- Workflow name
```

💡 Why these are required

- ✔️ \*\*Events (on)

Every workflow needs a trigger:

```yaml
on: push
```

✔️ Jobs (jobs)

A workflow must define at least one job:

```yaml
jobs:
  build:
    runs-on: ubuntu-latest
```

🧠 Minimum valid workflow

```yaml
on: push

jobs:
  example:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello"
```

---

### Q8: Which event is triggered by a webhook action from outside of the repository?

- webhook_dispatch
- api_dispatch
- workflow_dispatch
- remote_dispatch
- repository_dispatch

💡 https://docs.github.com/en/actions/using-workflows/events-that-trigger-workflows

✅ Correct answer:

```
- webhook_dispatch
- api_dispatch
- workflow_dispatch
- remote_dispatch
- repository_dispatch✅
👉 repository_dispatch
```

```yaml
on:
  repository_dispatch:
    types: [my-event]

curl -X POST \
  -H "Authorization: token <TOKEN>" \
  https://api.github.com/repos/OWNER/REPO/dispatches \
  -d '{"event_type":"my-event"}'
```

🧠 Key takeaway

- External trigger → repository_dispatch
- Manual trigger → workflow_dispatch
- If you want, I can show you a real-world integration example (e.g., trigger from your Next.js app) 👍

---

### Q9: Workflows are defined in which format

- YAML (.yml or .yaml)
- JSON
- TOML
- XML

✅ Correct answer:

```
- YAML (.yml or .yaml) ✅
- JSON
- TOML
- XML
```

---

### Q10: Where should you store sensitive data such as passwords or certificates that will be used in workflows

- In plaintext directly inside the workflow file
- In environment variables defined in the workflow file
- In GitHub Secrets ✅
- In the repository README file

✅ Correct answer:

```
- In plaintext directly inside the workflow file
- In environment variables defined in the workflow file
- In GitHub Secrets ✅
- In the repository README file
```

### Summary

#### GITHUB_TOKEN Summary

- **What it is:** An auto-generated, short-lived secret GitHub creates at the start of every workflow run. It authenticates your workflow with the GitHub API (e.g., creating releases, commenting on PRs, pushing commits) — no manual setup needed.

- **Permission levels:** Each scope can be set to `read`, `write`, or `none`.

- **Where you can set permissions:** At the workflow level or job level — but NOT at the step level.

- **Reusable workflows rule:** Permissions can only flow **downward** — a called workflow can restrict what it receives from the caller, but can never elevate it.
